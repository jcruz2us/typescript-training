(function run() {
  var alpha = 1;
  let beta = false;
  const charlie = "charlie";
  const delta = {};
  const echo = [];
  const foxtrot = [1];

  console.log("1 + false = ", alpha + beta);
  console.log("1 + {} = ", alpha + delta);

  // use a keyword (var, let, const)
  // give it an identifier
  // set the value
  // done
})();